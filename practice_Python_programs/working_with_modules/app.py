# modules are same ac creating UDF library in c or c++ or java

# pip is called as package manager and we can use pip to install library modules in our program using pip install
# library module name (pip install <module_name>)

# import docx
import usefull_tools

print(usefull_tools.roll_dice(6))
print(usefull_tools.get_file_ext("Vasu.txt"))
